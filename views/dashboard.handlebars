{{#if posts.length}}
<section>
  <h2>Recent Post</h2>
    {{#each posts as |post|}}
      {{> dashboard-post post}}
    {{/each}}

</section>
{{/if}}

<button type="submit" class="btn"><a class="link" href="/dashboard/create">Add New Post</a></button>